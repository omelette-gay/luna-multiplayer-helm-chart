apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "luna-multiplayer.fullname" . }}-config
  labels:
    {{- include "luna-multiplayer.labels" . | nindent 4 }}
{{- with .Values.config }}
data:
  {{- toYaml . | nindent 2 }}
{{- end }}
